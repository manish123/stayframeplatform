import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Knowledge Hub - StayFrame',
  description: 'Explore insights and articles on content creation.',
};

export default metadata;
